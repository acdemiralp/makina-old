#include <makina/renderer/backend/opengl/render_tasks/upload_points_render_task.hpp>

namespace mak
{
fg::render_task<upload_points_task_data>* add_upload_points_render_task(renderer* renderer)
{
  return nullptr;
}
}
